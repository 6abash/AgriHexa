#pragma once

// Copy this file to "secrets.h" (same folder) and fill in your own
// network credentials. secrets.h is gitignored so real credentials
// are never committed.

#define WIFI_SSID     "your_wifi_ssid"
#define WIFI_PASSWORD "your_wifi_password"
